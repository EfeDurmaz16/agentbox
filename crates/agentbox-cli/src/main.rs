// Agentbox CLI — user-facing commands.
//
// agentbox start    — start daemon (launchd on macOS)
// agentbox stop     — stop daemon
// agentbox status   — show daemon status, active shims, recent activity
// agentbox audit    — query audit log
// agentbox audit --tail  — live tail
// agentbox allow <domain> — add to network allowlist
// agentbox config   — show/edit config
//
// TODO: implement with clap derive macros

fn main() {
    println!("agentbox cli — not yet implemented");
}
