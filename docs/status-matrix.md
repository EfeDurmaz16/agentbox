# Agentbox Status Matrix

This file separates what is shipped in this repository from what is still
experimental, prototype, descriptor-only, unavailable, or planned. Agentbox is
moving from command interception into the AgentPod contract: adaptive governed
execution cells for autonomous agents. Not every provider boundary is
implemented yet. The canonical status terms live in the
[provider truth contract](provider-truth.md).

## Product Surface

| Surface | Status | Current proof |
|---------|--------|---------------|
| PATH shim interception | Shipped | `agentbox-shim` forwards command context to the daemon over a Unix socket. |
| Policy classifier | Shipped | `agentbox-policy` classifies allow, approve, and block decisions with workspace and config input. |
| AgentPod contract crate | Shipped | `agentbox-agentpod` is a first-class workspace crate for AgentPod provider ids, native receipt summary types, runner phase receipt vocabulary, enforcement status labels, and skipped-primitive vocabulary. Platform-specific loaders still live in `agentbox-daemon`. |
| Out-of-band approval | Shipped | The daemon can send ntfy approvals and waits for approve, deny, or timeout. |
| SQLite audit log | Shipped | The daemon records command, cwd, bucket, decision, parent process, and timing. |
| Hash-chained evidence | Shipped | New audit rows include `schema_version`, `prev_hash`, and `event_hash`. |
| Evidence export | Shipped | `agentbox evidence --limit N` exports audit rows as JSONL. `agentbox evidence --session <id> --bundle <dir>` writes a session evidence bundle directory with `index.json`, `bundle.json`, `manifest.json`, `replay.json`, `transcripts.json`, and `integrations.json`; `bundle.json` includes an optional AgentPod native receipt summary for `agentpod-*` sessions with provider, runner phase evidence, enforcement status, skipped planned primitives, and evidence refs; `agentbox evidence --session <id> --agentpod-receipt` prints that receipt summary from the session store, and `agentbox evidence --bundle <dir> --agentpod-receipt` prints it from a verified bundle directory; `index.json` carries a deterministic `root_sha256`; `agentbox evidence --verify --bundle <dir>` verifies the root hash, file hashes, and byte counts without needing the original session store. Integration descriptors for FIDES, AGIT, and OAPS are descriptor-only with `live_support=false` until external authority/adapters are configured. |
| Doctor command | Shipped | `agentbox doctor` reports daemon, shim, audit, PATH, provider readiness, remote AgentPod endpoint readiness, macOS native planning prerequisites such as Apple Virtualization plus future ES/NE entitlements, Linux native prototype prerequisites such as user namespaces, writable/delegated cgroups v2, seccomp, and Landlock ABI availability, and Windows native/VM-backed gaps such as Job Objects, AppContainer, WFP, ETW, and Sandbox/Hyper-V readiness. The daemon socket check and `agentbox status` distinguish missing sockets from stale socket files left behind when the daemon process is not running, and `agentbox clean` removes stale daemon pid/socket files before restart. `agentbox doctor --json` emits the same checks with `required_failed` versus `advisory_failed` counts so installers and release gates block on broken shipped setup without treating planned provider prerequisites as shipped requirements. |
| Setup plan command | Shipped | `agentbox setup-plan` and `agentbox setup-plan --json` derive the next local operator actions from the doctor report without mutating host state. `--provider direct-host|podman-compat|agentpod-macos|agentpod-linux|agentpod-windows|remote-agentpod` narrows the plan to one backend. Legacy `podman` remains a deprecated alias for `podman-compat`. Steps carry required/advisory severity, release-blocker truth, human action text, and a best-effort command when one is safe to suggest. |
| Setup command | Shipped partial | `agentbox setup` initializes local config and direct-host command shims. `agentbox setup --dry-run --wizard` prints an ordered guided setup flow, including first-run provider choice and provider bridge readiness inspection steps, and `agentbox setup --dry-run --wizard --json` emits `wizard_steps` plus `first_run_provider_plan` for installer UIs without mutating host state. `agentbox setup --dry-run --json` emits a machine-readable setup report, including deduplicated `operator_commands` from the readiness plan plus a scoped `agentbox bridge-health` command, and `--provider ...` scopes setup reporting to one backend. `--provider remote-agentpod --endpoint <https-or-gated-loopback-url>` validates the endpoint and emits exact export plus handshake commands without storing secrets. Provider-specific automatic installers are not implemented yet. |
| AgentPod manifest model | Shipped | `MinipodSpec` is now the compatibility type behind the AgentPod manifest surface. New manifests carry `schema_version`, `kind: AgentPod`, risk, workspace mode, filesystem, network, credentials, resources, services, labels, approvals, and task policy bundles. |
| Manifest policy validation | Shipped | Unsafe host env inheritance, host network mode, and protected mounts are rejected before provider create. |
| AgentPod workspace modes | Shipped partial | Manifests can declare `direct`, `overlay-review`, `ephemeral`, and `commit-gated` workspace modes. CLI-generated `agentbox run` and `agentbox minipod-spec` manifests now default medium, high, and very-high risk work to `overlay-review`; low-risk work and explicit `--workspace-mode direct` stay direct. Non-direct modes allocate validated upper/work paths and can materialize a projected review workspace before provider create. Linux native runner overlayfs apply is wired as a gated prototype; other native/provider workspace semantics are still incomplete. |
| AgentPod risk model | Shipped model | Manifests carry `low`, `medium`, `high`, or `very-high` risk intent for provider selection and evidence. |
| One-time credential file grants | Shipped | `--credential-file` creates read-only credential mounts and one-time file grants; provider mount metadata distinguishes them from ordinary read-only host mounts. |
| Explicit credential env grants | Shipped partial | `--credential-env name=HOST_ENV` records an explicit env grant and runtime exec injects only that named host env value into the command environment. Missing host env targets deny execution, one-time env grants are consumed after exposure, and transcripts redact credential-like output. Socket and provider-token grants remain manifest/authority metadata until provider-mediated sources are implemented. |
| Credential grant expiry | Shipped | Credential grants can carry `expires_at`; `agentbox run` and `agentbox minipod-spec` expose `--credential-ttl-seconds`; expired grants are removed before list/exec, not injected, and recorded as credential revocation evidence. |
| Credential grant operator commands | Shipped partial | `agentbox credentials <session>` lists persisted non-expired session grants and `agentbox credential-revoke <session> <name>` removes a grant from the session manifest while recording credential revocation evidence. This does not yet rotate upstream provider credentials or revoke external tokens. |
| Credential revocation evidence | Shipped | Destroying a runtime session records hash-chained credential revocation audit events for one-time grants. |
| Per-agent policy profiles | Shipped | `general`, `coding`, `research`, `deploy`, and custom profile ids can set policy defaults without hardcoding specific agent products. |
| Runtime session store | Shipped | Runtime sessions persist to a local JSON store. |
| Runtime manager | Shipped | Provider create, exec, status refresh, destroy, session persistence, and evidence events share one daemon-owned manager. |
| Sidecar readiness metadata | Shipped | Minipod services can carry command-based readiness probes; the Podman compatibility path starts sidecars and waits for probes before starting the workspace agent container. |
| Session-scoped approval grants | Shipped | Runtime sessions can carry approval grants that are persisted with the session and removed when the session is destroyed. |
| Approval scope enforcement | Shipped | Runtime manager exec enforces once, command, path, domain, and session approval grants for approve-bucket commands. Expired grants are ignored and block-bucket commands cannot be grant-bypassed. |
| Signed approval model | Shipped | Approval grants can be represented as signed approval records with evidence refs and optional FIDES-style signatures; no fake signing provider is shipped. |
| Governed host bridge schema | Shipped | Host bridge envelopes model command, file, credential, network, approval, evidence, and kill-switch requests across unix-socket, named-pipe, vsock, and remote-tunnel transports. Envelopes can be projected into redacted evidence events with stable request/transport kind metadata. |
| First-contact network mode | Shipped | `ApprovalOnFirstContact` is a first-class minipod network mode exposed through `--network-mode first-contact`. |
| Open-with-guardrails network mode | Shipped command mediation | General AgentPod manifests default to usable internet with metadata endpoints denied and private/LAN IP destinations mediated. Runtime command mediation allows unknown public HTTP in this mode while recording network boundary evidence; provider packet/domain enforcement is still reported separately. |
| Network denylist | Shipped | Minipod network policy and daemon policy config carry denied domains; denied network destinations are blocked before allowlists or approval grants. Cloud metadata endpoints such as AWS IMDS, ECS task credentials, GCP metadata, Alibaba metadata, and EC2 IPv6 IMDS are blocked by default command-mediation guardrails even if a broad allow override exists. |
| Localhost service policy | Shipped | Minipod manifests model localhost/loopback access and `--deny-localhost` makes runtime exec policy block loopback HTTP commands. |
| Network boundary evidence | Shipped | Runtime exec records network-specific audit events for allowed, blocked, and approval-required HTTP boundary decisions. |
| Network evidence export | Shipped | `agentbox evidence --network` exports only network boundary audit events as JSONL while preserving hash links and redaction. |
| Network explain CLI | Shipped | `agentbox network-explain <url>` explains URL policy buckets for a selected network mode without making the request and states that it is command mediation, not packet filtering. |
| Session network grants | Shipped | `agentbox network-grant <session> <domain>` adds a session-scoped domain approval grant for first-contact style HTTP commands. Denylists still win before grants. |
| Workspace review/apply/discard | Shipped partial | Runtime sessions can capture Git workspace diff snapshots, expose a machine-readable review action plan via `agentbox review --json`, print the same command-menu review skeleton via `agentbox review --tui`, export patches via `agentbox review --patch`, apply projected patches via `agentbox review-apply`, discard projected workspaces via `agentbox review-discard`, and create explicit lower-workspace commits via `agentbox review-commit`. These actions record workspace evidence; Linux native overlayfs mount wiring is prototype-only and other provider workspace semantics are still incomplete. |
| Command transcript export | Shipped | Runtime exec stores redacted stdout/stderr transcripts in the session evidence bundle with size metadata and truncation limits. |
| Session credential evidence | Shipped | Session evidence bundles include redacted credential grant summaries and credential audit events; `agentbox evidence --session <id> --credentials` exports just that credential evidence as JSONL. |
| Session replay metadata | Shipped | Session evidence bundles include ordered replay metadata with audit ids, hash links, policy buckets, decisions, and explicit metadata-only limitations. |
| Minipod manifest CLI | Shipped | `agentbox minipod-spec` generates and validates a deny-by-default manifest, including `--policy-bundle` task policy JSON files. |
| Run plan preview | Shipped | `agentbox run --plan` emits a JSON preview with selected provider metadata, selection reason, candidate providers, planned backend actions, warnings, network enforcement metadata, boundary primitive labels, primitive-level status/gate/scope metadata, and the full AgentPod manifest. It does not start a backend, hydrate credentials, create sessions, or execute the command. |
| Run JSON output | Shipped partial | `agentbox run --json` emits machine-readable session/run output for automation and avoids the interactive cleanup prompt for command runs. Actual execution still depends on a runnable backend such as Podman compatibility or gated Linux native prototype execution. |
| AgentPod session listing | Shipped partial | `agentbox pods` and the product-named alias `agentbox sessions` list persisted sessions with provider, status, risk, and agent. Both commands support `--json`, `--provider`, `--status`, and `--watch --interval-seconds N` for machine-readable output and watch-mode operator views. |
| Runtime provider registry | Shipped | `RuntimeProviderRegistry` can resolve AgentPod provider descriptors and compatibility providers, and can explain provider selection by risk and explicit provider hints. |
| Runtime provider listing | Shipped | `agentbox providers` reports family, platform, shipped/experimental/descriptor-only status, and network enforcement claims. `agentbox providers --json` exposes the same provider truth metadata plus boundary primitive labels, primitive-level status/gate/scope metadata, setup commands, doctor check names, verification commands, and bridge health for scripts, installers, and release checks. |
| Provider gap report | Shipped | `agentbox provider-gaps` and `agentbox provider-gaps --json` classify each provider's boundary primitives into active, prototype, descriptor-only, missing, and gated buckets using the same provider truth metadata as `agentbox providers --json`; it is an inspection surface and does not upgrade any descriptor-only primitive into an enforcement claim. |
| Provider readiness summary | Shipped | `agentbox provider-readiness` and `agentbox provider-readiness --json` join provider gap buckets with bridge readiness verdicts, next operator commands, doctor checks, and verification commands so installers and operators can inspect provider readiness without inferring it from multiple commands. |
| Bridge health CLI | Shipped | `agentbox bridge-health` and `agentbox bridge-health --json` report provider host-bridge support for policy, approval, credentials, evidence, kill switch, and network boundaries. `--provider <name>` narrows output to one backend. The command includes explicit readiness verdicts and claim boundaries; it reports support, active metadata, and gates, but does not prove provider execution by itself. |
| Network enforcement capability flags | Shipped | Runtime providers separately report active network enforcement strength, so planned policy support is not confused with packet/domain enforcement. |
| AgentPod provider truth | Shipped | `agentpod-macos`, `agentpod-linux`, `agentpod-windows`, and `remote-agentpod` expose capability/status metadata. `agentbox providers --json` reports planned boundary primitives such as Apple Virtualization, Linux namespaces/seccomp/Landlock, and Windows Job Objects/WFP/ETW, plus `boundary_primitive_statuses` entries that spell out descriptor/prototype status, required gates, active=false, and enforcement scope. Linux reports prototype primitives; macOS reports descriptor primitives and refuses provider execution until Apple Virtualization VM lifecycle, signed Endpoint Security, Network Extension lifecycle, and live allow/deny evidence tests exist; Windows remains descriptor-only; remote is experimental and only becomes available when `AGENTBOX_REMOTE_AGENTPOD_ENDPOINT` points at an HTTPS worker, or at loopback HTTP with `AGENTBOX_REMOTE_AGENTPOD_ALLOW_HTTP_LOOPBACK=1` for local verification. |
| Podman compatibility adapter | Shipped | `agentbox run` now routes through `RuntimeManager` and the Podman `RuntimeProvider` adapter, creating governed runtime sessions and evidence events. |
| AgentPod lifecycle CLI | Shipped partial | `agentbox pods` lists persisted runtime sessions and `agentbox stop-pod` stops runtime sessions through `RuntimeManager` before falling back to legacy Podman ids. Stopped sessions are retained for review and evidence, while transient approval grants are cleared. Runtime manager now rejects exec against stopped sessions before credential hydration or provider dispatch. |

## Runtime Backends

| Backend | Status | Notes |
|---------|--------|-------|
| Direct host with shims | Shipped | Strongest validated path today. It governs host-impacting shell commands but does not isolate all process behavior. `agentbox run --provider direct-host --risk low --json -- <cmd>` now creates a governed direct-host runtime session, executes argv commands through `RuntimeManager` policy checks, records evidence, and destroys the session in JSON mode. |
| AgentPod macOS | Descriptor plus VM runner request contract | Candidate surfaces include Apple Virtualization for local VM cells, Endpoint Security for host-event enforcement, and Network Extension for egress governance. `agentbox native-plan --provider agentpod-macos -- <cmd>` emits a secret-free VM/ES/NE execution plan with VM cell config, deterministic local storage layout under `~/.agentbox/agentpods/macos/<session>`, workspace mount, credential channel descriptors, bridge socket path, evidence spool path, required entitlement metadata, prerequisite checks, ordered runner phases, and an observed-only evidence observer descriptor for process, file, network, and provider lifecycle events. The daemon also exposes a typed macOS VM runner request schema with an embedded VM cell boot request contract, and a contract-only `agentbox-macos-vm-runner` binary that validates request JSON but refuses execution until Apple Virtualization lifecycle wiring exists. `AGENTBOX_MACOS_NATIVE=1` remains limited to native-plan and runner-request experiments; it does not make the `agentpod-macos` runtime provider available and CLI setup/first-run guidance does not suggest `agentbox run --provider agentpod-macos`. `agentbox native-plan --provider auto -- <cmd>` selects it on macOS hosts. The daemon models future ES exec/file authorization plus NE outbound flow requests mapped into host bridge policy requests. Provider execution intentionally returns unavailable until the VM runner can boot cells, a signed system extension, Network Extension lifecycle, and live allow/deny tests exist. See [Endpoint Security design](macos-endpoint-security.md), [system extension scaffold](macos-system-extension-scaffold.md), and [macOS minipod limitations](macos-minipod-limitations.md). |
| AgentPod Linux | Prototype runnable behind gate | Candidate surfaces include namespaces, no-new-privs, cgroups, Landlock, seccomp, eBPF, nftables, and overlayfs. Linux-only user, mount, PID namespace, no-new-privs, cgroups v2, seccomp, Landlock, isolation benchmark, eBPF observability descriptors/receipt templates, a composed native execution plan, a prototype BPF seccomp loader for supported syscall deny rules, a gated coarse connect-deny network guard for deny-all modes, a seccomp import descriptor for future external OCI/libseccomp profile ingestion, a Landlock path-beneath plan for read/write/create/remove/execute filesystem access with explicit workspace, mount, guest alias, and runtime support paths, an nftables egress policy descriptor with gated table lifecycle skeleton, plus OCI/libseccomp profile rendering, overlayfs workspace metadata and prototype overlayfs apply for review-required workspaces, and a gated prototype executor exist. `agentbox native-plan --provider agentpod-linux -- <cmd>` exposes the plan without execution, includes the cgroup v2 root used by live execution, and `agentbox native-plan --provider auto -- <cmd>` selects it on Linux hosts. Native runner phases now carry stable `agentpod.linux.runner.*` evidence event names, including an inactive network guard phase unless `AGENTBOX_LINUX_NETWORK_GUARD=1` is set for a deny-all mode and an inactive nftables skeleton phase unless `AGENTBOX_LINUX_NFTABLES=1` is set. eBPF observability receipts identify provider, session, cgroup path, PID/TGID fallback fields, and process/network event identity fields while staying `descriptor-only-or-unobserved` and `observed-only`. Successful `agentpod-linux` runtime exec records runner phase events into the session evidence/replay stream. Provider availability now requires Linux, `AGENTBOX_LINUX_NATIVE=1`, and side-effect-free host prerequisite checks for `unshare`, user namespaces, and a cgroups v2 root. On hosts that pass those gates, `agentbox run --provider agentpod-linux -- <cmd>` can use the prototype provider lifecycle, spawn the namespace wrapper, run `agentbox-linux-runner` inside `unshare`, bind-mount the host workspace or mount an overlayfs review workspace at the guest workspace path, install supported seccomp deny filters, optionally compile a coarse connect-deny network guard into seccomp for deny-all modes, install read/write/execute Landlock path-beneath filesystem rules after mount setup, attach the spawned process to the planned cgroup v2 directory, enforce command timeouts, collect stdout/stderr without shell wrapping, and clean up the cgroup after process exit. The seccomp profile import descriptor is metadata only for external OCI/libseccomp JSON today; the live loader still handles explicit Agentbox-generated syscall deny rules. The network guard has a Linux-only denied-destination fixture that proves `connect(2)` returns EPERM against a reachable loopback listener, but it is process-level deny-all connect coverage only; domain allowlists and packet firewall denial remain future nftables/eBPF work. The Landlock loader now has a Linux-only read/write/execute fixture and live smoke coverage, but it is still ABI v1 path-beneath enforcement with broad runtime support paths and is not a complete filesystem sandbox claim. The nftables live smoke creates, lists, and deletes an Agentbox-scoped table behind `AGENTBOX_LINUX_NFTABLES=1`; it does not attach egress hooks or prove packet/domain denial. The cgroup root defaults to `/sys/fs/cgroup` and can be redirected with `AGENTBOX_LINUX_CGROUP_ROOT` for delegated/test roots. `scripts/smoke-linux-native.sh` is the live native gate and now also exercises Landlock read/write/execute allow/deny fixtures, overlay-review lower-workspace preservation, overlay upper-layer capture, parallel native exec attempts, and runner request-file cleanup on Linux hosts that pass the prerequisite checks, including overlayfs availability. This is not a complete sandbox claim: eBPF probe loading/live capture, complete libseccomp profile loading, complete Landlock ABI coverage beyond the modeled path-beneath rules, packet/domain enforcement, and always-on cross-host overlayfs CI proof are not wired. |
| AgentPod Windows | Prototype primitives plus plan compiler | Candidate surfaces include Job Objects, AppContainer, WFP, ETW, Windows Sandbox, and Hyper-V primitives. A Windows Job Object plan/controller exists with Windows-only apply behavior, and `agentbox native-plan --provider agentpod-windows -- <cmd>` emits a secret-free Job Object/AppContainer/WFP/ETW/VM-boundary execution plan. The Job Object descriptor maps memory, CPU weight, risk-based process limits, timeout action metadata, a gated `AGENTBOX_WINDOWS_JOB_OBJECT=1` create/close smoke descriptor, and an explicit non-claim for live Win32 apply proof. `scripts/smoke-windows-job-object.sh` calls `CreateJobObjectW` and `CloseHandle` on Windows when gated; it skips elsewhere and does not prove process assignment, kill-on-close cleanup, or limit enforcement. The AppContainer descriptor carries workspace mode, write policy, overlay/review metadata, read-only or read-write mount descriptors, protected path rules, and an explicit non-claim for live ACL proof. The WFP descriptor compiles loopback, private/LAN mediation, manifest domain allow/deny entries, and evidence event names without claiming packet/domain denial proof. The ETW plan includes observed-only session correlation, evidence schemas for process/network/provider lifecycle events, planned export bundle files, redaction policy, spool path, hash-chain algorithm, and bundle-root linkage, without claiming live capture or enforcement. The VM boundary includes workspace mount metadata, named-pipe or Hyper-V socket bridge endpoints, credential channel descriptors, guest evidence spool path, and shutdown policy; these are runner contracts, not live VM lifecycle proof. The plan also carries an AgentPod native receipt descriptor using the same provider/enforcement/runner-phase/evidence-ref vocabulary as native receipts while keeping `descriptor-only-or-unobserved` status. `agentbox native-plan --provider auto -- <cmd>` selects it on Windows hosts. Provider execution intentionally remains unavailable until live Windows tests prove process tree cleanup, filesystem/credential constraints, network evidence or denial, and evidence export. See [Windows native provider](windows-native-provider.md). |
| Remote AgentPod | Experimental gated | The `remote-agentpod` descriptor models remote/disposable worker execution over a remote bridge. `agentbox remote-descriptor` emits a secret-free transport/auth/evidence/lifecycle descriptor with required events, kill switch, heartbeat interval, and restart policy metadata; `agentbox remote-handshake` emits a secret-free signed-challenge descriptor; `agentbox remote-evidence` emits validated evidence upload metadata without uploading it; `agentbox remote-evidence-upload --session <id>` posts verified bundle envelopes to HTTPS workers by deriving worker metadata from the local session; `agentbox remote-evidence-stream --session <id>` uploads UTF-8 evidence files as ordered stream chunks using the same persisted metadata; `agentbox remote-approval-grant --session <id>` resolves a pending command approval by deriving remote worker metadata from the local session and minting an explicit command-scope grant; `agentbox remote-evidence-status --session <id>` queries a worker evidence status route by deriving the worker endpoint and worker-side session id from persisted remote session metadata, including heartbeat, restart policy, kill-switch, and evidence-sealed state; `agentbox remote-evidence-status --session <id> --agentpod-receipt` joins status with the lifecycle journal and prints an AgentPod-style remote receipt summary with lifecycle events, sealed evidence state, bundle refs, stream refs, and explicitly unsupported credential modes; `agentbox remote-events --session <id>` queries the persisted session lifecycle journal with monotonic event sequences; `agentbox remote-worker-status --session <id>` queries worker supervision state by deriving the worker endpoint from persisted session metadata; `agentbox remote-restart --session <id>` explicitly resumes a stopped or failed worker session using the same persisted metadata without replaying the previous command; `agentbox remote-exec --session <id> -- <argv>` sends a new argv command through the typed remote transport for an existing worker session; `agentbox remote-workspace-export --session <id>` pulls a worker workspace into a local review directory using the same persisted metadata and validating the exported bundle; `agentbox minipod-inspect <session>` prints session-scoped remote operator commands when metadata exists; explicit endpoint/worker overrides remain available; and `agentbox remote-workspace-apply` applies a pulled export to a local workspace after verifying the manifest and file hashes. `agentbox remote-evidence --bundle-dir <dir>` derives the upload hash and event count from a verified evidence bundle root and marks the request with bundle provenance metadata. The daemon also has an in-memory transport conformance contract plus an HTTPS adapter for handshake/create/exec/evidence/evidence-bundle/evidence-stream/approval-grant/evidence-status/lifecycle-events/worker-status/workspace-export/restart/destroy response validation, including Ed25519 challenge signature verification, legacy canonical challenge binding, optional hash-bound workspace bundles, loopback-only HTTP dev transport when explicitly gated, and required destroy-time `KillSwitchAck` when the kill switch is required. With `AGENTBOX_REMOTE_AGENTPOD_ENDPOINT=https://...`, `RemoteAgentPodProvider` can handshake, create a session, persist worker metadata, optionally build and send a filtered workspace bundle when `AGENTBOX_REMOTE_AGENTPOD_WORKSPACE_BUNDLE=1`, hand off explicit env credentials plus bounded file credential payloads, execute direct argv commands, destroy through a worker transport, and seal the final stopped-session evidence bundle to the worker after local destroy audit evidence is written. `agentbox-remote-worker` is a contract worker binary that signs handshakes, advertises `NetworkPolicy`, responds to the expected worker routes, prepares the requested workspace during create-session, materializes verified workspace bundle files, materializes verified file credential payloads only under the session workspace path, removes one-time file credentials from worker metadata and unlinks their materialized files after command completion, exports the current workspace as a verified bundle, enforces session AgentPod command policy before spawning a process, records a persisted lifecycle journal for allocation, command, evidence, approval, restart, and destroy events, honors manifest-bound non-expired command/path/domain/session approval grants for approval-bucket commands while refusing one-time grant consumption, records pending approval metadata when approval-bucket commands lack a matching grant, accepts command-scope approval grants only when they match a pending command, accepts session-bound environment credential grant metadata, exposes materialized file credential paths through `AGENTBOX_CREDENTIAL_FILE_<NAME>`, rejects arbitrary command environment material without a matching grant, rejects socket/provider-token credential grants until those handoff protocols exist, requires a created running worker session before exec, records command supervision counters and last command result in session status, records worker boot count/previous boot/recovered session supervision state at `/worker/status` and inside evidence status, explicitly resumes stopped or failed sessions without replaying commands, records evidence upload receipts with bundle provenance metadata for matching sessions, stores AgentboxEvidenceBundleUpload envelopes only after recomputing indexed file hashes, byte counts, and bundle roots when `--state-dir` is set, accepts ordered evidence stream chunks with per-chunk hashes and sealed stream hashes, exposes a session-bound evidence status route with stream, pending approval, redacted credential status, heartbeat, restart policy, kill-switch, and evidence-sealed state, persists stored bundle hash/byte/path references, stream state, file credential metadata, pending approval state, and lifecycle events in worker session snapshots, fails state-changing requests when worker state cannot be persisted, persists worker session snapshots when `--state-dir` is set, reloads persisted running sessions after worker restart, defaults exec to the session workspace, refuses explicit working directories outside that workspace, runs allowed direct argv commands without a shell, and maps destroy to a running-command kill signal; `scripts/smoke-remote-worker.sh` proves real CLI/provider execution against a gated loopback worker plus provider env and file credential handoff, direct loopback HTTP handshake, create, exec, deny-by-default worker policy rejection, evidence upload, CLI evidence stream chunk upload, pending approval grant resolution, evidence status inspection with command counters, stream state, heartbeat, restart policy, kill-switch state, evidence-sealed state, lifecycle event journal inspection, workspace export, CLI workspace pullback and local apply, envelope-verified bundle payload storage, persisted receipt/stored-bundle/stream state, CLI worker supervision status, restart reload with worker supervision boot count, explicit stopped-session restart, post-restart CLI exec, and destroy-time kill behavior. Sandboxed remote execution, rich worker-side approval prompts, full live event streaming, socket/provider-token credential handoff, daemon-synchronized credential consumption, richer workspace merge UX, and automatic worker process restart orchestration are not implemented. See [Remote AgentPod](remote-agentpod.md). |
| Podman compatibility minipods | Experimental | The runtime provider ID is `podman-compat`; legacy `podman` is only a deprecated CLI alias. `agentbox run` uses the daemon-owned runtime manager path. `agentbox pods` lists persisted runtime sessions and `agentbox stop-pod` stops sessions through `RuntimeManager` before falling back to legacy Podman ids. Non-direct workspace modes can prepare projected review workspaces with review/apply/discard commands, but native provider overlayfs semantics are not complete. `scripts/smoke-podman-bridge.sh` remains the live gate for socket/shim bridge proof; it resolves or builds a Linux ELF guest shim and skips with exit 77 when Podman or the Linux target artifact is unavailable. The provider resolves Linux guest shim artifacts through `AGENTBOX_LINUX_SHIM`, `.linux` sidecar artifacts, or common Rust target paths, and rejects non-Linux shim binaries before injecting them into Linux containers. |

## Ecosystem Integrations

| Surface | Status | Notes |
|---------|--------|-------|
| FIDES credential authority hook | Shipped | Agentbox exposes a FIDES-compatible credential authority request/decision skeleton without hard dependency on the FIDES runtime. The default hook requires external authority and does not fake approval. |
| agit evidence lineage | Shipped skeleton | Agentbox can map runtime audit events into AGIT-style lineage records with optional commit and workspace diff refs. The default publisher requires an external AGIT adapter and does not claim live integration. |
| Switchboard coordination | Planned | Switchboard can coordinate multiple agents, while Agentbox owns each local runtime boundary. No direct integration is shipped yet. |
| Aspendos consumer path | Planned | Aspendos-style general agents are target consumers for governed minipods. No bundled Aspendos runtime integration is shipped yet. |

## Current Direction

Agentbox is not trying to be a smaller VM manager. The target product is:

```text
agent intent
  -> governed minipod manifest
  -> local runtime provider
  -> filesystem / network / credential / process boundaries
  -> policy and approval decisions
  -> tamper-evident evidence
```

Podman is allowed to remain a compatibility backend, but AgentPod providers are
the product direction and should become the real enforcement layer.

See [glossary](glossary.md) for the canonical meanings of AgentPod, minipod,
boundary, policy, authority, evidence, provider, and host bridge.
See [provider truth](provider-truth.md) for the canonical provider support
terms used by docs, release notes, and CLI metadata.
See [AgentPod contract](agentpod-contract.md) for the final product shape:
adaptive providers, workspace modes, credential grants, network policy, host
bridge, approval, and evidence.
See [Mac mini replacement wedge](mac-mini-replacement-wedge.md) for the local
software-boundary positioning and its limits.
See [250+ commit product sprint](roadmap-250-commits.md) for the current
execution queue that supersedes the original 100-issue planning cut.
See [product checkpoint audit](product-checkpoint-audit.md) for the current
runnable, prototype, descriptor-only, skipped, and next-proof boundary.
See [release readiness](release-readiness.md) for the checklist before tagging
public builds.
See [v0.2 demo checklist](v0.2-demo-checklist.md) for the public demo path and
claims to avoid.
See [installer packaging](installer-packaging.md) for the packaging path and the
rule against shipping unverified installers.
See [macOS minipod limitations](macos-minipod-limitations.md) for the current
VM-backed boundary and native enforcement gap.
See [safe file sharing](safe-file-sharing.md) for current workspace, read-only
mount, credential, and system bridge guidance.
See [safe credential patterns](safe-credential-patterns.md) for task-scoped
credential grants, redaction limits, and FIDES authority handoff.
See [Linux eBPF observability](linux-ebpf-observability.md) for kernel event
evidence design that is not yet enforcement.
See [network enforcement limits](network-enforcement-limits.md) for the
platform-specific line between classification, observation, provider network
mode, and packet/domain enforcement.
See [threat model](threat-model.md), [platform isolation strategy](platform-isolation.md),
and [public limitations](limitations.md) for the current public boundary.
